---
import MainLayout from "../layouts/MainLayout.astro";
import { getFirstPost } from "../lib/api";

const firstPost = await getFirstPost();
---

<MainLayout title="HomePAge">
  <h1>Astro & Sanity web</h1>

  {firstPost.map((post) => <h3>{post.title}</h3>)}
</MainLayout>
